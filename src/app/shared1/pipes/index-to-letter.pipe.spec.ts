import { IndexToLetterPipe } from './index-to-letter.pipe';

describe('IndexToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
